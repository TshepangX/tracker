<%= form_with model: @region do |form| %>
   <div>
      <%= form.label :title %><br>
      <%= form.text_field :title, required: true %>
   </div>

   <div>
   <%= form.submit%>
   </div>
<% end %>   