    <h3><%= link_to "Home", root_path(@animals) %></h3>

<h1> New Region </h1>

<%= form_with model:@region do |form| %>
    <div>
        <%= form.label :title %><br>
        <%= form.text_field :title %>
    </div>

    <div>
        <%= form.submit %>
    </div>
<% end %>